phase: 'Phase 2 — Per-Post Inventory + register_post Tool + Dev.to Key Guard'
certified_floor: 132/0/0
what_is_next: 'Phase 3 — (pending)'
notes: 'Phase 2 complete: Refactored InventoryManager from monolithic inventory.yaml to per-post YAML files in data/inventory/. InventoryManager now scans data/inventory/*.yaml — one file per post. Added add_post() method. Added register_post MCP tool to draft_tools.py. Added scheduled_date fallback in publish_to_wordpress (reads from inventory YAML when not explicitly passed). Migrated 7 posts (dev-001 through dev-007) to individual YAML files. data/inventory.yaml retired to data/inventory.yaml.bak. Updated conftest.py inventory fixture for dir-based InventoryManager. Rewrote test_inventory.py. Bug fixes: excerpt kwarg in create_draft, category string filtering in publisher. Dev.to guard: publish_devto returns {"error": "DEVTO_API_KEY not configured"} when key is missing — no HTTP call made. 2 new publisher tests. Test runtime 11.70s.'
